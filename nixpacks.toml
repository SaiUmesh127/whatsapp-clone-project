[phases.setup]
nixPkgs = ['maven', 'jdk17']

[phases.build]
cmds = ['mvn clean package -DskipTests']

[start]
cmd = 'java -Dserver.port=$PORT -Dspring.profiles.active=prod -jar target/whatsapp-clone-1.0.0.jar'