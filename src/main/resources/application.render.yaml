services:
  - type: web
    name: whatsapp-backend
    env: java
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -Dserver.port=$PORT -Dspring.profiles.active=render -jar target/*.jar
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: render
